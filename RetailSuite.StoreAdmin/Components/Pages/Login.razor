@page "/login"
@using RetailSuite.StoreAdmin.Shared
@inject AuthService Auth
@inject NavigationManager Nav
@layout LoginLayout

<div class="login-container">
    <div class="login-card">

        <h2>RetailSuite</h2>
        <p class="subtitle">Admin Login</p>

        <input class="input full" @bind="email" placeholder="Email" />
        <input class="input full" type="password" @bind="password" placeholder="Password" />

        <button class="btn btn-primary full" @onclick="LogIn">
            Login
        </button>

    </div>
</div>

@code {
    string? email;
    string? password;
    // string? error;

    async Task LogIn()
    {

        // TEMP: fake login
        Auth.SetToken("dummy-token");

        Nav.NavigateTo("/");

        // var success = await Auth.LoginAsync(email, password);

        // if (!success)
        // {
        //     error = "Invalid login.";
        //     return;
        // }

        // Nav.NavigateTo("/");
    }
}